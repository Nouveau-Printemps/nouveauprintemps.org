diff --git a/backend/config.go b/backend/config.go
index 6052e4b..c97fa21 100644
--- a/backend/config.go
+++ b/backend/config.go
@@ -37,6 +37,7 @@ type Config struct {
 	Language      string   `toml:"language"`
 	Database      string   `toml:"database"`
 	AdminPassword string   `toml:"admin_password"`
+	HomeContent   string   `toml:"home_content"`
 
 	Sections []Section `toml:"section"`
 
@@ -78,6 +79,7 @@ func (c *Config) DefaultValues() {
 	c.PublicFolder = "public"
 	c.Database = "database.sqlite"
 	c.AdminPassword = "Ch@ngeM€Please!"
+	c.HomeContent = "home"
 	c.Quotes = []string{"Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do."}
 	c.Replacers = []Replacer{{"~", "&thinsp;"}}
 }
diff --git a/backend/home.go b/backend/home.go
index f824a69..3d3100e 100644
--- a/backend/home.go
+++ b/backend/home.go
@@ -18,6 +18,7 @@ var (
 type homeData struct {
 	*data
 	Sections []*Section
+	Content  template.HTML
 }
 
 func (h *homeData) SetData(d *data) {
@@ -31,6 +32,27 @@ func HandleHome(r *chi.Mux) {
 		if d == nil {
 			return
 		}
+		if c, ok := rootContent["home"]; ok {
+			d.Content = c.Content
+		} else {
+			path := filepath.Join(cfg.RootFolder, cfg.HomeContent+".md")
+			b, err := os.ReadFile(path)
+			if err != nil {
+				if os.IsNotExist(err) {
+					notFound(w, r)
+					return
+				}
+				panic(err)
+			}
+			d.Content, ok = parse(b, new(EntryInfo), d.data)
+			if !ok {
+				w.WriteHeader(http.StatusInternalServerError)
+				return
+			}
+			rd := new(rootData)
+			rd.Content = d.Content
+			rootContent["home"] = rd
+		}
 		d.handleGeneric(w, r, "home", d)
 	})
 }
diff --git a/backend/templates/home.html b/backend/templates/home.html
index d574525..4bf1fc5 100644
--- a/backend/templates/home.html
+++ b/backend/templates/home.html
@@ -4,6 +4,7 @@
     <h1>{{ .Name }}</h1>
     <p>{{ .PageDescription }}</p>
   </div>
+  <section>{{ .Content }}</section>
   <div class="sections">
     {{ range .Sections }}
     <section>
-- 
2.51.2

